# https://zenn.dev/haxibami/scraps/083718c1beec04
strict-peer-dependencies=false

registry=https://registry.npmjs.org

# NOTE: pnpm 11 no longer reads `public-hoist-pattern` from .npmrc — those live in
# pnpm-workspace.yaml (publicHoistPattern) now. See that file for the hoist list.
package-manager-strict=false